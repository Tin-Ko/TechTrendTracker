# Scraper (link harvest stays in Scrapy; content fetch is plain requests+lxml)
scrapy>=2.11
requests>=2.31
lxml>=5.0

# RabbitMQ client (scraper -> extraction worker)
pika>=1.3

# LLM skill extraction
ollama>=0.2          # local Gemma 4 via Ollama (default path)
openai>=1.30         # cloud DeepSeek fallback in extractor.py

# Title embedding (bge-small-en-v1.5 ONNX, 384-d)
onnxruntime>=1.17
tokenizers>=0.15
numpy>=1.26

# Supabase Postgres (pgvector inserts)
psycopg[binary]>=3.1
